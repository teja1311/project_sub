Problem 1 Explanation 

we can create all the possible substrings, and then we can just print the substring which has greater lenght

We have one string, just like we reach to each item in a list or array, we should iterate over the string.
we need to create substrings as we are looping through the string and break whenever the repeated char occurs.

writing the solution function in Problem_1.py